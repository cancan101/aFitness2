import { StyleSheet } from 'react-native';

const styles = {
  listItemImage: {
    width: 40,
    height: 40,
    marginBottom: -15,
    marginTop: -15,
  },
};

export default StyleSheet.create(styles);
